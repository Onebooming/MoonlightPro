syntax = "proto3";

package MoonLight;

// 定义登录请求的数据结构
message LoginRequest {
    string username = 1;
    string password = 2;
}

// 定义登录响应的数据结构
message LoginResponse {
    bool success = 1;
    string session_id = 2;
    string message = 3;
}